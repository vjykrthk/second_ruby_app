<%= form_for @micropost do |f| %>
	<%= render 'shared/error_messages', :f => f.object %>
	<div class="field">
		<%= f.text_area :content %>
	</div>
	<div class="action">
		<%= f.submit "Submit" %>
	</div>
<% end %>