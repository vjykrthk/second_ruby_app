<div class="user_info">
	<a href="<%= user_path(current_user) %>">
		<table>
			<tr>
				<td><%= gravatar_for current_user, :size=>30 %></td>
				<td><span><%= current_user.name %></span></br>
		       <span><%= pluralize(current_user.microposts.count, "micropost") %></span></td>
			</tr>
		</table>
		

	</a>
</div>